# Leave empty / omit for SERVE_CLIENT (UI + API on one port — same-origin /api/v1).
# Do not bake a LAN IP or production domain into the client build.
# VITE_API_URL=https://asset.refexone.com/api/v1
#
# Local Vite only (proxy target port for `npm run dev`):
VITE_API_PORT=3053
