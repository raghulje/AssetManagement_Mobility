# Refex UI (React)

Modern frontend for Refex IT Asset Management, wired to the Node API in `../server`.

## Run

```bash
npm install
npm run dev
```

- UI: http://localhost:5173
- API: http://localhost:3001
- Login: `admin` / `password`
