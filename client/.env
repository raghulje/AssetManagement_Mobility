# Leave empty / omit for SERVE_CLIENT (UI + API on one port — same-origin /api/v1).
# Do not bake a LAN IP into the client build; production uses the browser origin
# (or set VITE_API_URL only if the API is on a different host).
# VITE_API_URL=https://itam.your-domain.com/api/v1
VITE_API_PORT=3001
